/* Configuración compartida por index.html (clientes) y admin.html (Sergio) */
const CONFIG = {
  NOMBRE: 'Sergio Escobar',
  WHATSAPP: '',      // Formato internacional sin +, ej: 56912345678
  EMAIL: '',         // Correo para recibir solicitudes (opción "Enviar por correo")
  SHEETS_URL: '',    // URL /exec del Google Apps Script (ver README)
  VALIDEZ_DIAS: 5    // Vigencia de la cotización en el PDF
};
